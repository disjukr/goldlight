# 2d basic rect

Draws animated colored rectangles with the built-in 2D scene model.

From this directory:

```sh
bun run goldlight dev
```

Build a production bundle:

```sh
bun run goldlight build
```
