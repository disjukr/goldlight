# 2d strokes

Shows the old `render_strokes` study on the built-in runtime path node.
`dashArray` and `dashOffset` are now wired into the runtime API, but the renderer does not apply dash tessellation yet.

From this directory:

```sh
bun run goldlight dev
```
