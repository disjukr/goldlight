# 2d auto text transforms

Animated `auto` text inside transformed groups. The scene continuously changes
translation, rotation, and scale so the runtime can switch between direct-mask,
transformed-mask, SDF, and path fallback behavior.
