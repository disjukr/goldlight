# 3d basic triangle

Draws a simple animated triangle with the built-in 3D scene model.

From this directory:

```sh
bun run goldlight dev
```

Build a production bundle:

```sh
bun run goldlight build
```
