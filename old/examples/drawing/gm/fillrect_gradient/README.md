# Fillrect Gradient

Gradient fill GM snapshot derived from the Skia reference case.

Run the goldlight snapshot from the repository root:

```sh
bun examples/drawing/gm/fillrect_gradient/main.ts
```

Run the CanvasKit comparison snapshot:

```sh
bun examples/drawing/gm/fillrect_gradient/canvaskit_main.ts
```
