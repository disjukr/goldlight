# Render Glyph Clusters

This example shapes sample text through `@disjukr/goldlight/text`, builds glyph clusters, and writes `out.png`.

Run from the repository root:

```sh
bun run build:text:native
bun examples/text/render_glyph_clusters/main.ts
```

Optional verification:

```sh
bun run typecheck
```
