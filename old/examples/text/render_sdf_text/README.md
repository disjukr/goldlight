# Render SDF Text

This example shapes text through `@disjukr/goldlight/text`, converts glyph masks into SDF coverage, and writes `out.png`.

Run from the repository root:

```sh
bun run build:text:native
bun examples/text/render_sdf_text/main.ts
```

Optional verification:

```sh
bun run typecheck
```
