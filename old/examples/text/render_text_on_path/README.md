# Render Text On Path

This example shapes text through `@disjukr/goldlight/text`, places glyphs along guide curves, and writes `out.png`.

Run from the repository root:

```sh
bun run build:text:native
bun examples/text/render_text_on_path/main.ts
```

Optional verification:

```sh
bun run typecheck
```
